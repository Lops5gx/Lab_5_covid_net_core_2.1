@model COVID19.Models.DadoCovid

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>DadoCovid</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="id" />
            <div class="form-group">
                <label asp-for="PaisesId" class="control-label"></label>
                <select asp-for="PaisesId" class="form-control" asp-items="ViewBag.PaisesId"></select>
                <span asp-validation-for="PaisesId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
